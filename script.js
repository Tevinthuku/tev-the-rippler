$(document).ready(function() {
  $(".rippler").rippler({
    effectClass      :  'rippler-effect'
    ,effectSize      :  8      // Default size (width & height)
    ,addElement      :  'body'   // e.g. 'svg'(feature)
    ,duration        :  400
  });
});